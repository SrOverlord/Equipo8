<!DOCTYPE html>
<html>
  <head>
    <title>Local</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>


  <header class="ContMenu" >

    <div class="contLogo">
      <h1>Home Bank</h1>

    </div>

    <div class="contSesion">
    <% if user_signed_in? %>
    
    <div class= "contSaldo">
    <h3>Saldo:</h3>
    <% @cuenta = CuentaBanca.where(user_id: current_user.id ).first() %>
    <%# <% @cuenta = CuentaBanca.select(:saldo) %>
    <h3><%=  @cuenta.attributes["saldo"] %></h3>
    <%# <%= render @valor%> 
    </div>
    <% else %>
    <li><h2><%= link_to 'Iniciar sesion ', new_user_session_path %></h2></li>
    <% end %>
    </div>


  </header>
<% if user_signed_in? %>
  <div class= "menu">
   <li><%= link_to "CuentaBancos", :cuenta_bancas %></li>
 <li><%= link_to "Criptos",  screencripto_path %></li>
    <li><%= link_to 'Terminar Sesion', destroy_user_session_path, :method => :delete %></li>
 <% end %>
    <body>
  </div>
  
    <%= yield %>
  </body>
</html>
